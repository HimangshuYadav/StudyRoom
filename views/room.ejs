<%- include('partials/head', { title: (room ? room.name : 'Room') + ' — StudyRoom' }) %>
<%- include('partials/navbar') %>
<link rel="stylesheet" href="/css/room.css">

<div id="toast-container"></div>

<div class="room-workspace" data-room-id="<%= room._id %>">

  <!-- ── Room Header ── -->
  <header class="room-header">
    <div class="room-header-info">
      <div class="room-header-title" id="workspace-title"><%= room.name %></div>
      <div class="room-header-meta">
        <span class="room-topic-tag">
          <svg class="icon icon-sm"><use href="#icon-book-open"/></svg> <%= room.topic %>
        </span>
        <span class="code-pill" id="workspace-code" title="Click to copy">
          <svg class="icon icon-sm"><use href="#icon-hash"/></svg> <span id="code-value"><%= room.joinCode %></span>
        </span>
        <span class="members-pill">
          <svg class="icon icon-sm"><use href="#icon-users"/></svg> <span id="members-count">0</span> online
        </span>
      </div>
    </div>
    <a href="/dashboard" class="nav-item back-link">
      <svg class="icon icon-sm"><use href="#icon-arrow-left"/></svg> Back
    </a>
  </header>

  <!-- ── Chat ── -->
  <section class="chat-panel">
    <div class="chat-messages" id="chat-messages"></div>

    <div class="typing-indicator hidden" id="typing-indicator">
      <span id="typing-text"></span>
    </div>

    <div class="chat-input-area">
      <form class="chat-input-form" id="chat-input-form">
        <input
          type="text"
          id="chat-message-input"
          placeholder="Write something thoughtful…"
          autocomplete="off"
          maxlength="1000"
        >
        <button type="submit" class="btn-send" aria-label="Send">
          <svg class="icon icon-sm"><use href="#icon-send"/></svg>
        </button>
      </form>
    </div>
  </section>

  <!-- ── Sidebar ── -->
  <aside class="room-sidebar">

    <!-- Online Members -->
    <div class="online-panel">
      <div class="panel-title">
        <span class="live-dot"></span>
        In this room
      </div>
      <div class="online-list" id="online-list"></div>
    </div>

    <!-- Tabs -->
    <div class="sidebar-tabs">
      <div class="tab-bar">
        <button class="tab-btn active" data-tab="notes">
          <svg class="icon icon-sm"><use href="#icon-pencil"/></svg> Notes
        </button>
        <button class="tab-btn" data-tab="ai">
          <svg class="icon icon-sm"><use href="#icon-cpu"/></svg> AI Tutor
        </button>
      </div>

      <!-- ── Multi-Topic Notes Panel ── -->
      <div class="tab-panel notes-panel active" id="panel-notes">
        
        <!-- Filter Tabs: All, Public, Private -->
        <div class="notes-filter-bar">
          <div class="filter-pills">
            <button class="filter-pill active" data-filter="all">All</button>
            <button class="filter-pill" data-filter="public">
              <svg class="icon icon-sm"><use href="#icon-eye"/></svg> Public
            </button>
            <button class="filter-pill" data-filter="private">
              <svg class="icon icon-sm"><use href="#icon-lock"/></svg> Private
            </button>
          </div>
          <button class="btn-new-topic" id="btn-new-topic" title="Create new note topic">
            <svg class="icon icon-sm"><use href="#icon-plus"/></svg> New Topic
          </button>
        </div>

        <!-- Inline New Topic Form (hidden by default) -->
        <form class="new-topic-form hidden" id="new-topic-form">
          <input
            type="text"
            id="new-topic-title"
            class="topic-input"
            placeholder="Topic title (e.g. Physics Formulas)"
            maxlength="60"
            required
            autocomplete="off"
          >
          <div class="topic-form-footer">
            <label class="visibility-toggle">
              <input type="checkbox" id="new-topic-public" checked>
              <span>Public (Room)</span>
            </label>
            <div class="topic-form-btns">
              <button type="button" class="btn-cancel" id="btn-cancel-topic">Cancel</button>
              <button type="submit" class="btn-create-topic">Create</button>
            </div>
          </div>
        </form>

        <!-- Topic Selector Dropdown -->
        <div class="topic-selector-wrapper">
          <select id="note-topic-select" class="topic-select">
            <option value="">Loading notes…</option>
          </select>
          <span id="notes-status" class="saved">Saved</span>
        </div>

        <!-- Active Note Header Bar -->
        <div class="active-note-header">
          <div class="active-note-title-wrap">
            <span class="vis-badge public" id="active-note-vis">
              <svg class="icon icon-sm"><use href="#icon-eye"/></svg> Public Note
            </span>
          </div>
          <button class="btn-icon danger" id="btn-delete-note" title="Delete this note topic">
            <svg class="icon icon-sm"><use href="#icon-trash"/></svg>
          </button>
        </div>

        <!-- Text Editor -->
        <textarea
          id="shared-notes-editor"
          placeholder="Take notes here — updates live for everyone in public notes…"
        ></textarea>

        <div class="notes-footer-row">
          <label class="btn-attach" title="Attach file to this note">
            <input type="file" id="note-file-input" class="hidden-file-input">
            <svg class="icon icon-sm"><use href="#icon-paperclip"/></svg> Attach File
          </label>
          <div class="notes-char-count"><span id="notes-char-count">0</span> characters</div>
        </div>

        <!-- File Attachments List -->
        <div class="attachments-section" id="attachments-section">
          <div class="attachments-title">Files & Attachments (<span id="attachments-count">0</span>)</div>
          <div class="attachments-list" id="attachments-list"></div>
        </div>

      </div>

      <!-- ── AI Tutor Panel ── -->
      <div class="tab-panel ai-panel" id="panel-ai">

        <div class="ai-section">
          <div class="ai-section-title">
            <svg class="icon icon-sm"><use href="#icon-list"/></svg> Session Summary
          </div>
          <button class="btn-ai summarize" id="ai-summarize-btn">Summarise Room</button>
          <div class="ai-output" id="ai-summary-output"></div>
        </div>

        <div class="ai-divider"></div>

        <div class="ai-section">
          <div class="ai-section-title">
            <svg class="icon icon-sm"><use href="#icon-lightbulb"/></svg> Ask the Tutor
          </div>
          <input type="text" class="ai-input" id="ai-doubt-input" placeholder="What don't you understand?">
          <button class="btn-ai explain" id="ai-explain-btn">Get Explanation</button>
          <div class="ai-output" id="ai-doubt-output"></div>
        </div>

        <div class="ai-divider"></div>

        <div class="ai-section">
          <div class="ai-section-title">
            <svg class="icon icon-sm"><use href="#icon-target"/></svg> Practice Quiz
          </div>
          <input type="text" class="ai-input" id="ai-quiz-topic-input" placeholder="Topic (optional — uses notes if blank)">
          <button class="btn-ai quiz" id="ai-quiz-btn">Generate Quiz</button>
          <div id="ai-quiz-output"></div>
        </div>

      </div>
    </div>
  </aside>

</div>

<%- include('partials/footer') %>
<script src="/js/chat.js"></script>
<script src="/js/notes.js"></script>
<script src="/js/ai.js"></script>
